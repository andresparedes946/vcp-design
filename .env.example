# Dominio propio del sitio, sin barra final.
# Sólo hace falta cuando tengas un dominio: si no está, se usa la URL que
# Vercel asigna sola (vcp-design.vercel.app).
# Cargala en Vercel > Settings > Environment Variables, entorno Production.
NEXT_PUBLIC_SITE_URL=https://vcpdesign.com
